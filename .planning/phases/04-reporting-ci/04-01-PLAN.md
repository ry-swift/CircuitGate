---
phase: 04-reporting-ci
plan: 01
type: execute
wave: 1
depends_on: ["03-01"]
files_modified:
  - packages/core/src/report/report-model.ts
  - packages/core/src/report/markdown-renderer.ts
  - packages/core/src/report/json-renderer.ts
  - packages/cli/src/index.ts
  - action.yml
  - packages/github-action/
autonomous: true
requirements: [RPT-01, RPT-02, RPT-03, RPT-04, CI-01, CI-02, CI-03, CI-04]
user_setup:
  - "发布正式 GitHub Action 前需要创建 GitHub 仓库和 release token；本阶段可先本地验证 action。"
must_haves:
  truths:
    - "CLI 可以输出 Markdown 和 JSON 报告。"
    - "报告包含 summary、findings、evidence、recommendations。"
    - "GitHub Action 可以在 PR 中运行并上传 artifact。"
  artifacts:
    - "packages/core/src/report/markdown-renderer.ts"
    - "packages/core/src/report/json-renderer.ts"
    - "action.yml"
  key_links:
    - "PR comment 必须来自同一份 ReportModel，避免 CLI 和 CI 结果不一致。"
---

<objective>
把 CircuitGate 从本地检查器变成工程师可日常使用的审查工作流。

Purpose: 让报告可读、可追溯、可在 PR 中出现，从而进入真实协作流程。  
Output: ReportModel、Markdown/JSON renderer、GitHub Action 和 PR comment。
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: 设计 ReportModel</name>
  <files>packages/core/src/report/report-model.ts</files>
  <read_first>packages/core/src/findings/finding.ts</read_first>
  <action>定义报告模型，包含 project、profile、runMetadata、summary、findingsBySeverity、blockingFindings、waivers、rawArtifacts。ReportModel 是所有输出格式的唯一来源。</action>
  <verify>为 Finding 到 ReportModel 的聚合写测试。</verify>
  <acceptance_criteria>
    - critical/high/medium/low/info 统计准确。
    - rawArtifacts 能链接 KiCad 原始输出。
  </acceptance_criteria>
  <done>报告层拥有稳定中间模型。</done>
</task>

<task type="auto">
  <name>Task 2: 实现 Markdown 和 JSON 报告</name>
  <files>packages/core/src/report/markdown-renderer.ts, packages/core/src/report/json-renderer.ts</files>
  <read_first>PRODUCT.md</read_first>
  <action>Markdown 报告必须包含项目概览、风险汇总、阻塞问题、完整 finding 列表、waiver 指南和复查清单。JSON 报告必须适合 CI 和后续 Web 读取。</action>
  <verify>快照测试 Markdown 输出，schema 测试 JSON 输出。</verify>
  <acceptance_criteria>
    - 每条 finding 显示 severity、rule ID、证据、影响、建议。
    - Markdown 中没有未绑定证据的 AI 式泛化结论。
  </acceptance_criteria>
  <done>用户可以直接阅读报告。</done>
</task>

<task type="auto">
  <name>Task 3: 实现 GitHub Action</name>
  <files>action.yml, packages/github-action/</files>
  <read_first>packages/cli/src/index.ts</read_first>
  <action>创建 GitHub Action wrapper，支持 project、profile、fail-on-severity、comment、upload-artifact 参数。PR comment 只展示新增 critical/high 风险摘要和 artifact 链接。</action>
  <verify>使用本地 action 测试或 act 验证 action 参数传递。</verify>
  <acceptance_criteria>
    - action.yml 参数完整。
    - report artifact 被上传。
    - fail-on-severity 可以控制 CI 是否失败。
  </acceptance_criteria>
  <done>CircuitGate 可以进入 PR 工作流。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test` passes.
- [ ] `circuitgate review examples/blinky --format markdown --output review.md` 生成报告。
- [ ] GitHub Action 本地或测试仓库运行成功。
</verification>

<success_criteria>
- 用户不用打开源码也能理解风险。
- CI 和本地输出一致。
- 报告足够好，可以作为付费审查服务交付底稿。
</success_criteria>

<output>
After completion, create `.planning/phases/04-reporting-ci/04-01-SUMMARY.md`
</output>
