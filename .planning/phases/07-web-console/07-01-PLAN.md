---
phase: 07-web-console
plan: 01
type: execute
wave: 1
depends_on: ["06-01"]
files_modified:
  - apps/web/
  - packages/core/src/report/report-model.ts
  - packages/server/
  - db/schema.sql
autonomous: true
requirements: [WEB-01, WEB-02, WEB-03]
user_setup:
  - "需要选择部署平台、对象存储和数据库；默认可先用本地开发环境。"
must_haves:
  truths:
    - "用户可以查看报告历史。"
    - "用户可以管理 finding waiver。"
    - "用户可以比较两个版本报告的风险差异。"
  artifacts:
    - "apps/web/"
    - "packages/server/"
    - "db/schema.sql"
  key_links:
    - "Web 读取的报告必须兼容 Phase 4 的 JSON ReportModel。"
---

<objective>
在已经有付费信号后，构建轻量 Web Console，服务团队协作和报告历史。

Purpose: 支持团队复查、waiver 管理和版本比较，但不替代 CLI/CI 主工作流。  
Output: Web app、API、数据库 schema、报告上传和 waiver 功能。
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@business/pilot-tracker.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: 设计数据模型和 API</name>
  <files>db/schema.sql, packages/server/</files>
  <read_first>packages/core/src/report/report-model.ts</read_first>
  <action>定义 Project、ReviewRun、Finding、Waiver、Artifact、TeamMember 数据模型。API 支持上传 JSON report、读取报告、创建 waiver、比较两个 review runs。</action>
  <verify>API 集成测试覆盖上传、读取、waiver、diff。</verify>
  <acceptance_criteria>
    - ReportModel 版本字段被保存。
    - Waiver 必须包含 reason 和 author。
  </acceptance_criteria>
  <done>Web 后端可以承接 CLI/CI 报告。</done>
</task>

<task type="auto">
  <name>Task 2: 构建报告 UI</name>
  <files>apps/web/</files>
  <read_first>PRODUCT.md</read_first>
  <action>实现报告列表、风险汇总、finding detail、waiver 操作和版本 diff。界面应偏工业工具风格，信息密度高、少装饰、重点突出阻塞风险和证据。</action>
  <verify>运行本地页面并检查桌面/移动布局没有文本溢出。</verify>
  <acceptance_criteria>
    - 用户能按 severity 过滤。
    - finding detail 显示 evidence、recommendation、waiver。
    - diff 页面显示新增、解决、仍存在风险。
  </acceptance_criteria>
  <done>团队可以通过 Web 复查报告。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] API tests pass.
- [ ] Web build passes.
- [ ] 手动验证上传报告、创建 waiver、查看 diff。
</verification>

<success_criteria>
- Web Console 解决团队协作问题，不膨胀成完整 EDA。
- CLI/CI 仍然可独立运行。
- 报告历史和 waiver 开始形成数据资产。
</success_criteria>

<output>
After completion, create `.planning/phases/07-web-console/07-01-SUMMARY.md`
</output>

