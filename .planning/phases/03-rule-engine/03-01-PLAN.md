---
phase: 03-rule-engine
plan: 01
type: tdd
wave: 1
depends_on: ["02-01"]
files_modified:
  - packages/core/src/rules/rule-schema.ts
  - packages/core/src/rules/rule-runner.ts
  - packages/core/src/rules/rule-result.ts
  - rules/power/
  - rules/interface/
  - rules/pcb/
  - rules/bom/
  - rules/manufacturing/
autonomous: true
requirements: [RULE-01, RULE-02, RULE-03, RULE-04]
user_setup: []
must_haves:
  truths:
    - "规则可以通过 schema 校验。"
    - "至少 20 条首批规则可以运行。"
    - "规则结果可以合并 KiCad findings。"
  artifacts:
    - "packages/core/src/rules/rule-schema.ts"
    - "packages/core/src/rules/rule-runner.ts"
    - "rules/"
  key_links:
    - "规则输出必须转换为统一 Finding。"
---

<objective>
建立 CircuitGate 的核心护城河：可扩展、可测试、可解释的硬件规则库。

Purpose: 把工程知识从代码中抽离，形成可持续积累的规则资产。  
Output: rule schema、rule runner、首批规则和测试夹具。
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@rules/rule-candidates.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: 定义规则 schema</name>
  <files>packages/core/src/rules/rule-schema.ts</files>
  <read_first>PRODUCT.md, rules/rule-candidates.md</read_first>
  <action>定义规则 DSL 的字段：id、title、category、severity、appliesTo、inputs、evidence、message、recommendation、falsePositiveNotes。用 JSON Schema 或 Zod 校验规则文件。</action>
  <verify>为有效规则、缺失 severity、缺失 evidence 三类样例写测试。</verify>
  <acceptance_criteria>
    - 无效规则在启动时失败并指出规则 ID。
    - schema 不允许缺失 severity、message、recommendation。
  </acceptance_criteria>
  <done>规则文件可被稳定加载和校验。</done>
</task>

<task type="auto">
  <name>Task 2: 实现 rule runner</name>
  <files>packages/core/src/rules/rule-runner.ts, packages/core/src/rules/rule-result.ts</files>
  <read_first>packages/core/src/findings/finding.ts, packages/core/src/intake/design-manifest.ts</read_first>
  <action>实现规则加载、上下文构造、规则执行、Finding 转换和结果合并。runner 必须支持按 category、severity、profile 过滤。</action>
  <verify>测试多个规则并行运行、规则失败隔离、KiCad finding 合并。</verify>
  <acceptance_criteria>
    - 单条规则异常不会中断全部检查。
    - 合并后 finding 保留 source=rule 或 source=kicad。
  </acceptance_criteria>
  <done>规则引擎可接入 CLI。</done>
</task>

<task type="auto">
  <name>Task 3: 实现首批规则</name>
  <files>rules/power/, rules/interface/, rules/pcb/, rules/bom/, rules/manufacturing/</files>
  <read_first>rules/rule-candidates.md</read_first>
  <action>实现至少 20 条规则，优先选择可由 manifest、BOM、Gerber 文件清单、KiCad 输出可靠判断的规则。避免需要复杂电路语义推理的规则进入首批。</action>
  <verify>`circuitgate rules validate` 和 `circuitgate review examples/blinky` 都能运行。</verify>
  <acceptance_criteria>
    - 五类规则都有覆盖。
    - 每条规则都有测试样例或 fixture。
  </acceptance_criteria>
  <done>规则库可以产生真实报告价值。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test` passes.
- [ ] `circuitgate rules validate` passes.
- [ ] 至少 20 条规则被加载并执行。
</verification>

<success_criteria>
- 规则库可扩展且可测试。
- 规则 finding 与 KiCad finding 使用同一数据结构。
- 首批规则能覆盖 Phase 1 发现的高频痛点。
</success_criteria>

<output>
After completion, create `.planning/phases/03-rule-engine/03-01-SUMMARY.md`
</output>
