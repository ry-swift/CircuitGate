---
phase: 05-ai-review-assistant
plan: 01
type: execute
wave: 1
depends_on: ["04-01"]
files_modified:
  - packages/core/src/ai/ai-provider.ts
  - packages/core/src/ai/evidence-pack.ts
  - packages/core/src/ai/review-summary.ts
  - packages/core/src/report/markdown-renderer.ts
autonomous: true
requirements: [AI-01, AI-02, AI-03]
user_setup:
  - "使用云模型时需要用户提供 API key；无 API key 时 AI 功能必须自动跳过。"
must_haves:
  truths:
    - "AI 只能读取 evidence pack，不能访问未结构化的完整私密设计文件。"
    - "关闭 AI 后全部非 AI 功能仍然工作。"
    - "AI 摘要明确标注为辅助解释。"
  artifacts:
    - "packages/core/src/ai/evidence-pack.ts"
    - "packages/core/src/ai/review-summary.ts"
  key_links:
    - "AI summary 必须引用 finding IDs。"
---

<objective>
增加 AI 审查摘要，但保持工业软件需要的证据边界和可关闭能力。

Purpose: 提升报告可读性，而不牺牲可靠性、隐私和可追溯性。  
Output: AI provider adapter、evidence pack、review summary 和安全 prompt。
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: 设计 evidence pack</name>
  <files>packages/core/src/ai/evidence-pack.ts</files>
  <read_first>packages/core/src/report/report-model.ts</read_first>
  <action>从 ReportModel 生成最小 evidence pack，只包含 finding ID、severity、rule ID、message、evidence 摘要、recommendation 和 raw output 引用。不要把完整设计文件传给模型。</action>
  <verify>测试 evidence pack 不包含设计文件全文和超出 finding 的数据。</verify>
  <acceptance_criteria>
    - evidence pack 可序列化。
    - 每个 AI 可引用事实都有 finding ID。
  </acceptance_criteria>
  <done>AI 输入边界清晰。</done>
</task>

<task type="auto">
  <name>Task 2: 实现 AI provider adapter</name>
  <files>packages/core/src/ai/ai-provider.ts, packages/core/src/ai/review-summary.ts</files>
  <read_first>PRODUCT.md</read_first>
  <action>实现可插拔 provider，支持 disabled、openai-compatible 两种模式。prompt 明确禁止生成未在 evidence pack 中出现的关键结论，输出必须包含 referencedFindingIds。</action>
  <verify>用 fake provider 测试关闭、成功、模型错误三种路径。</verify>
  <acceptance_criteria>
    - 没有 API key 时不失败，只跳过 AI summary。
    - AI 输出缺少 referencedFindingIds 时被拒绝或降级。
  </acceptance_criteria>
  <done>AI 不影响核心审查可靠性。</done>
</task>

<task type="auto">
  <name>Task 3: 接入报告渲染</name>
  <files>packages/core/src/report/markdown-renderer.ts</files>
  <read_first>packages/core/src/ai/review-summary.ts</read_first>
  <action>在 Markdown 报告顶部加入可选 AI Summary，明确标注“辅助解释，事实以规则和工具证据为准”。每条 AI 结论展示 referenced finding IDs。</action>
  <verify>快照测试包含 AI 和不包含 AI 的报告。</verify>
  <acceptance_criteria>
    - 用户可以通过 `--no-ai` 关闭。
    - 报告中 AI 段落不会覆盖 severity。
  </acceptance_criteria>
  <done>AI 摘要可用且边界明确。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test` passes.
- [ ] `circuitgate review examples/blinky --no-ai` 正常运行。
- [ ] 使用 fake provider 时报告包含带 finding ID 的 AI Summary。
</verification>

<success_criteria>
- AI 增强可读性，但不改变事实来源。
- 用户可以完全不用 AI。
- 隐私和证据边界被代码结构 enforce。
</success_criteria>

<output>
After completion, create `.planning/phases/05-ai-review-assistant/05-01-SUMMARY.md`
</output>
