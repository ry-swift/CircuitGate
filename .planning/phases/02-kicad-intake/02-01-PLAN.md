---
phase: 02-kicad-intake
plan: 01
type: tdd
wave: 1
depends_on: ["01-01"]
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - packages/cli/src/index.ts
  - packages/core/src/intake/design-manifest.ts
  - packages/core/src/kicad/kicad-cli.ts
  - packages/core/src/findings/finding.ts
  - examples/blinky/
autonomous: true
requirements: [INTK-01, INTK-02, INTK-03, KICD-01, KICD-02, KICD-03, KICD-04]
user_setup:
  - "本机或 CI 环境需要安装 KiCad CLI；未安装时 CLI 必须输出清晰错误。"
must_haves:
  truths:
    - "用户可以运行 circuitgate review examples/blinky。"
    - "系统可以生成 DesignManifest。"
    - "系统可以调用 KiCad ERC/DRC 或在缺失 KiCad 时给出可诊断错误。"
    - "KiCad 输出被转换成统一 Finding。"
  artifacts:
    - "packages/cli/src/index.ts"
    - "packages/core/src/intake/design-manifest.ts"
    - "packages/core/src/kicad/kicad-cli.ts"
    - "packages/core/src/findings/finding.ts"
  key_links:
    - "CLI 输出必须使用 core 层的 DesignManifest 和 Finding，而不是临时字符串拼接。"
---

<objective>
做出 CircuitGate 的第一条可运行技术主线：输入项目，调用 KiCad，输出结构化结果。

Purpose: 让产品从文档变成可演示的本地工具。  
Output: CLI skeleton、DesignManifest、KiCad CLI adapter、Finding 数据结构和示例项目。
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: 初始化 TypeScript monorepo 和 CLI</name>
  <files>package.json, pnpm-workspace.yaml, packages/cli/src/index.ts</files>
  <read_first>PRODUCT.md</read_first>
  <action>建立最小 pnpm workspace，包含 packages/cli 和 packages/core。CLI 提供 `circuitgate review <path> --profile <name> --format json|markdown --output <file>` 参数解析，先允许输出 JSON。</action>
  <verify>`pnpm test` 和 `pnpm build` 可以运行；`circuitgate review --help` 显示命令说明。</verify>
  <acceptance_criteria>
    - package.json 包含 build、test、lint 脚本。
    - CLI 参数包含 project path、profile、format、output。
  </acceptance_criteria>
  <done>CLI 可以被本地调用。</done>
</task>

<task type="auto">
  <name>Task 2: 实现 DesignManifest</name>
  <files>packages/core/src/intake/design-manifest.ts</files>
  <read_first>.planning/REQUIREMENTS.md</read_first>
  <action>实现输入扫描逻辑，识别 `.kicad_pro`、`.kicad_sch`、`.kicad_pcb`、Gerber zip、BOM CSV、pick-and-place CSV。输出 DesignManifest，包含 found、missing、warnings 和 inferredProjectType。</action>
  <verify>为 KiCad 目录、Gerber zip、缺失文件三种输入写单元测试。</verify>
  <acceptance_criteria>
    - DesignManifest 字段稳定并有类型定义。
    - 缺失关键文件时不崩溃，返回 warning。
  </acceptance_criteria>
  <done>后续规则和报告可以依赖统一 manifest。</done>
</task>

<task type="auto">
  <name>Task 3: 实现 KiCad CLI adapter 和 Finding 归一化</name>
  <files>packages/core/src/kicad/kicad-cli.ts, packages/core/src/findings/finding.ts</files>
  <read_first>KiCad CLI 文档或本地 `kicad-cli --help` 输出</read_first>
  <action>封装 KiCad ERC/DRC 调用，保存 stdout/stderr 和输出文件路径。定义 Finding 类型：id、source、severity、ruleId、title、message、evidence、recommendation、waiver。</action>
  <verify>用 mock KiCad 输出测试 parser，不要求测试环境必须安装 KiCad。</verify>
  <acceptance_criteria>
    - KiCad 缺失时返回 diagnostic finding，而不是未捕获异常。
    - 原始输出路径保存在 finding evidence 中。
  </acceptance_criteria>
  <done>工具输出可被报告层复用。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test` passes.
- [ ] `pnpm build` passes.
- [ ] `circuitgate review examples/blinky --format json` 输出 manifest 和 findings。
</verification>

<success_criteria>
- Phase 2 所有关联需求可验证。
- CLI 已能跑通最小闭环。
- 没有把业务真相写在报告字符串里。
</success_criteria>

<output>
After completion, create `.planning/phases/02-kicad-intake/02-01-SUMMARY.md`
</output>
