{
  "name": "circuitgate",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r build && node --test packages/*/dist/**/*.test.js"
  },
  "dependencies": {
    "@circuitgate/cli": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.4.2"
  }
}
